/**
 * Twitter card image — re-exports the OG image so both share the same visual.
 * Next.js uses this file convention for `twitter:image` meta tags.
 */
export { default, alt, size, contentType } from "./opengraph-image";
